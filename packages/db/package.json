{
  "name": "@novel-assistant/db",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "rebuild:native:node": "npm rebuild better-sqlite3",
    "pretest": "npm rebuild better-sqlite3",
    "test": "vitest run",
    "posttest": "pnpm --filter @novel-assistant/desktop rebuild:native",
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "@novel-assistant/core": "workspace:*",
    "better-sqlite3": "^11.7.0",
    "uuid": "^11.0.3"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.12",
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
